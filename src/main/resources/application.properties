#Spring hibernate
spring.jpa.hibernate.ddl-auto=update

# Spring jdbc
spring.datasource.url=jdbc:mysql://localhost/spring_demo_db
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# Spring security settings - out of the box features
security.user.name=armaandhir
security.user.password=springdemo

# Spring actuator settings
endpoints.health.id=status
endpoints.health.sensitive=false 
endpoints.shutdown.enabled=true
endpoints.shutdown.sensitive=false

management.context-path=/actuators
management.security.roles=SYSADMIN